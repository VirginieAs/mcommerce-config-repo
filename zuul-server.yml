server:
  port: 9004

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9102/eureka/

ribbon:
  ReadTimeout: 10000

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false